import random


top_of_range = input("type a number: ")

#checks if the variable top_of_range contains a string that represents a valid integer, and if it does, it converts that string to an integer 
if top_of_range.isdigit():
    top_of_range = int(top_of_range)




random_number = random.randint(11)
print(r)


